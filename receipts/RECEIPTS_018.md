- Timestamp: 2026-07-28T14:33
- Summary: Fixed Home Screen widget inflation crash in Launcher.
- Files: 
  - widget_media.xml
  - Custom drawable files
- Actions:
  - The "transparent block with fix button" means the Launcher (like Smart Launcher) crashed with an `InflateException` when trying to apply the `RemoteViews`. This exception happens in the Launcher's process, which is why it wasn't caught in our `LogKeeper`.
  - Found that `<View>` was used as a divider. `<View>` is explicitly NOT supported by `RemoteViews` and causes `ClassNotFoundException`/`InflateException`. Replaced `<View>` with `<FrameLayout>`.
  - Replaced direct hex color strings (e.g., `#1A2196F3`) in `android:background` and `android:divider` with proper `@drawable/` XML shape resources (`widget_divider`, `widget_search_bg`, `widget_divider_solid`), as some older/custom launchers throw exceptions when parsing direct hex colors in `RemoteViews`.
- Verification: Compiled and verified locally.
- Timestamp: 2026-07-29T10:20:00Z
- Summary: Added refresh button to widget, removed manual mode toggle button from top bar, and fixed PlaybackService compile errors.
- Files touched:
  - app/src/main/res/layout/widget_media.xml
  - app/src/main/java/com/example/widget/MediaWidgetProvider.kt
  - app/src/main/java/com/example/widget/MediaWidgetService.kt
  - app/src/main/java/com/example/service/PlaybackService.kt
- What was actually done:
  - Removed `widget_btn_mode` from widget layout.
  - Added `widget_btn_refresh` to widget layout next to loop and shuffle buttons.
  - Handled `ACTION_REFRESH` intent in `MediaWidgetProvider` to call `notifyAppWidgetViewDataChanged`.
  - Removed `ACTION_TOGGLE_MODE` handling from `MediaWidgetProvider`.
  - Fixed syntax errors and `lateinit` errors in `PlaybackService.kt`.
- Verification: local build only
- Timestamp: 2026-07-29T12:05:00Z
- Summary: Implemented RightTools button sizing updates to make them smaller and compact with a gap from playback buttons.
- Files touched:
  - app/src/main/java/com/example/ui/screens/PlayerScreen.kt
- What was actually done:
  - Modified the IconButtons within RightTools to have a 36.dp size modifier.
  - Modified the Icons within RightTools to have a 20.dp size modifier.
- Verification: local build only
